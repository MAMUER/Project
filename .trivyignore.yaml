# Trivy misconfiguration ignore rules
# Infrastructure components that legitimately need writable filesystems

- rule: AVD-KSV-0014
  paths:
    - configs/k8s/deployments/postgres.yaml

- rule: AVD-KSV-0014
  paths:
    - configs/k8s/deployments/rabbitmq.yaml
