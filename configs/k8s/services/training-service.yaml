apiVersion: v1
kind: Service
metadata:
  name: training-service
  namespace: fitness-platform
spec:
  selector:
    app: training-service
  ports:
  - port: 50053
    targetPort: 50053
  type: ClusterIP