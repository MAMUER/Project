apiVersion: v1
kind: Service
metadata:
  name: biometric-service
  namespace: fitness-platform
spec:
  selector:
    app: biometric-service
  ports:
  - port: 50052
    targetPort: 50052
  type: ClusterIP