{
  "name": "fitness-platform-load-test",
  "version": "1.0.0",
  "description": "Load tests for fitness platform",
  "scripts": {
    "test": "k6 run load-test.js",
    "test:local": "k6 run -e BASE_URL=http://localhost:8080 load-test.js",
    "test:prod": "k6 run -e BASE_URL=https://your-domain.com load-test.js"
  },
  "devDependencies": {
    "k6": "^0.0.0"
  }
}